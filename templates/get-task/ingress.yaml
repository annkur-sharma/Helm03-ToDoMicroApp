apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-get-task-ingress-{{ .Release.Name }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.getTask.ingress.host }}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: todo-get-task-svc-{{ .Release.Name }}
            port: 
              number: 80
