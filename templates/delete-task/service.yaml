apiVersion: v1
kind: Service
metadata:
  name: todo-delete-tasks-svc-{{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: todo-delete-tasks-{{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 8000
