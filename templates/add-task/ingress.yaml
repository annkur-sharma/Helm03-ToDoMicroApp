apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-add-tasks-ingress-{{ .Release.Name }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.addTask.ingress.host }}
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-add-tasks-svc-{{ .Release.Name }}
                port:
                  number: 80
